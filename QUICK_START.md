# 動作確認ガイド（データベース無し版）

このガイドでは、Supabaseデータベース無しでアプリケーションを動作確認する方法を説明します。

## ✅ 現在の状態

- **開発サーバー**: 起動済み（http://localhost:3000）
- **データモード**: モックデータ（データベース不要）
- **環境変数**: 不要（.env.localファイルなしで動作）

## 🚀 起動方法

すでに開発サーバーは起動していますが、再起動する場合:

```bash
npm run dev
```

ブラウザで http://localhost:3000 を開いてください。

## 📋 動作確認手順

### 1. ダッシュボード（トップページ）
**URL**: http://localhost:3000

**確認項目**:
- ✅ 統計カード（稼働中の工程、作業者、設備稼働率、遅延工程）が表示される
- ✅ 4つのグラフ（生産効率、工程ステータス、生産量、不良率）が表示される
- ✅ 進行中の工程テーブルが表示される
- ✅ 左サイドバーのナビゲーションが表示される

### 2. ガントチャート
**URL**: http://localhost:3000/gantt

**確認項目**:
- ✅ 工程のタイムライン表示
- ✅ 進捗バーの表示
- ✅ フィルタリング機能（ドロップダウン）
- ✅ 生産オーダー一覧テーブル

### 3. カンバンボード
**URL**: http://localhost:3000/kanban

**確認項目**:
- ✅ 4つの列（待機、進行中、承認待ち、完了）
- ✅ タスクカードの表示
- ✅ ドラッグ&ドロップでカード移動可能
- ✅ 統計カード（各列のタスク数）

### 4. カレンダー
**URL**: http://localhost:3000/calendar

**確認項目**:
- ✅ 月次カレンダービュー
- ✅ 色分けされたイベント表示
- ✅ イベントクリックで詳細モーダル表示
- ✅ ビュー切り替え（月/週/日）

### 5. 工程管理
**URL**: http://localhost:3000/processes

**確認項目**:
- ✅ 工程一覧テーブル
- ✅ 検索機能
- ✅ ステータス・進捗・優先度の表示
- ✅ 担当者・設備の表示

### 6. 作業者管理
**URL**: http://localhost:3000/workers

**確認項目**:
- ✅ 作業者カード表示
- ✅ スキル・資格の表示
- ✅ 稼働状況の表示
- ✅ 連絡先情報

### 7. 在庫管理
**URL**: http://localhost:3000/inventory

**確認項目**:
- ✅ 在庫一覧テーブル
- ✅ 在庫状況バー
- ✅ 在庫不足アラート（赤背景）
- ✅ 統計カード（総在庫、不足アイテム、総額）

### 8. 品質管理
**URL**: http://localhost:3000/quality

**確認項目**:
- ✅ 品質統計カード（合格率、不良率）
- ✅ 検査結果分布グラフ
- ✅ 工程別検査グラフ
- ✅ 検査一覧テーブル

### 9. 設備管理
**URL**: http://localhost:3000/equipment

**確認項目**:
- ✅ 設備ステータスカード
- ✅ 設備詳細カード（稼働状況、メンテナンス情報）
- ✅ メンテナンスアラート表示
- ✅ 故障アラート表示

### 10. レポート
**URL**: http://localhost:3000/reports

**確認項目**:
- ✅ KPIメトリクスカード（4つ）
- ✅ 4つのグラフ（生産効率、生産量、労働時間、設備停止時間）
- ✅ 月次実績サマリーテーブル
- ✅ フィルタリング機能

## 🔍 モックデータの確認

すべてのデータは `lib/mockData.ts` から読み込まれています:

```typescript
// 例: 工程データ
export const mockProcesses: Process[] = [
  {
    id: 'P001',
    name: '部品A 切削加工',
    status: '進行中',
    progress: 65,
    // ...
  },
  // ...
];
```

## 🎨 確認ポイント

### ✅ 正常動作の確認
- ページ遷移がスムーズ
- グラフやチャートが正しく表示される
- テーブルのデータが表示される
- レスポンシブデザイン（画面サイズ変更）

### ⚠️ エラーが出ていないことを確認
- ブラウザのコンソール（F12）でエラーがないこと
- ページが真っ白になっていないこと
- 「Loading...」で止まっていないこと

## 🛠️ トラブルシューティング

### ページが表示されない
```bash
# サーバーを再起動
Ctrl+C で停止
npm run dev
```

### エラーが出る
```bash
# 依存パッケージを再インストール
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### ポート3000が使用中
```bash
# プロセスを確認
lsof -i :3000

# プロセスを終了
kill -9 <PID>

# または別のポートで起動
PORT=3001 npm run dev
```

## 📝 データベース接続について

**現在はデータベース不要です**が、将来的にSupabaseに接続する場合:

1. `.env.local` ファイルを作成
2. Supabase認証情報を設定
3. 自動的にSupabaseモードに切り替わります

詳細は [SUPABASE_SETUP.md](./SUPABASE_SETUP.md) を参照してください。

## ✨ 動作確認完了後

すべての機能が正常に動作していることを確認したら:

1. 各ページの機能を試す
2. データの表示を確認
3. UI/UXの確認
4. レスポンシブデザインの確認（モバイル/タブレット）

問題がなければ、データベース連携の実装に進むことができます。

## 🎯 次のステップ

1. ✅ モックデータでの動作確認（現在のステップ）
2. Supabaseプロジェクトの作成
3. データベーススキーマの実行
4. 環境変数の設定
5. データベース連携の動作確認

---

**開発サーバー起動中**: http://localhost:3000

すべてのページを確認して、問題なく動作することを確認してください！
